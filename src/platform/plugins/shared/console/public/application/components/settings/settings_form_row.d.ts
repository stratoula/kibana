import React from 'react';
export interface DevToolsSettingsModalProps {
    label: string;
    children: React.ReactNode;
}
export declare const SettingsFormRow: ({ label, children }: DevToolsSettingsModalProps) => React.JSX.Element;
