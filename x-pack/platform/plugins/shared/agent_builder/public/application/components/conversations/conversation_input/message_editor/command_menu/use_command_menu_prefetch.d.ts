/**
 * Prefetches data for all command menus on first invocation.
 * Returns a callback that should be called when the editor receives focus.
 */
export declare const useCommandMenuPrefetch: () => () => void;
